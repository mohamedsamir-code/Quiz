import 'package:flutter/material.dart';

class LowerTabbar extends StatelessWidget {
  const LowerTabbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Image(
      image: AssetImage(
        'assets/plank-exercise.png',
      ),
    );
  }
}
